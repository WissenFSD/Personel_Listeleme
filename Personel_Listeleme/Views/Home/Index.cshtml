@model List<ViewList>


@for (int i = 0; i <= Model.Count/3; i++)
{
	string src;
	string url;
	int id = 0;
	{
		src = Model[i].Gender == "F" ? "/css/woman.png" : "/css/man.png";
		id = Model[i].Id;
		url = "https://localhost:7256/Home/Detail/?id=" + id;
	}

	<div class="container text-center">
		<div class="row align-items-start">

			@for (int k = 1; k <= 3; k++)
			{
				{
					src = Model[i].Gender == "F" ? "/css/woman.png" : "/css/man.png";
					id = Model[i].Id;
					url = "https://localhost:7256/Home/Detail/?id=" + id;
				}
				i++;
				<div class="col">
					<div class="card divMain" style="width: 18rem;">
					<img class="card-img-top img" src='@src' alt="Card image cap">
					<div class="card-body">
						<h5 class="card-title">@Model[i].JobTitle</h5>
						<a href='@url' class="btn btn-primary">Detay..</a>
					</div>
				</div>
			</div>
				
			}
	</div>
		</div>
}